import {Home,CalendarDays,Trophy,User,ClipboardList} from 'lucide-react';import {NavLink,Outlet} from 'react-router-dom';
const tabs=[['/app/home','홈',Home],['/app/calendar','캘린더',CalendarDays],['/app/draw-result','결과',ClipboardList],['/app/leaderboard','리더보드',Trophy],['/app/profile','내 정보',User]] as const;
export function AppLayout(){return <div className="min-h-dvh bg-parchment text-ink"><main className="mx-auto min-h-dvh max-w-xl pb-24"><Outlet/></main><nav className="fixed inset-x-0 bottom-0 border-t border-zinc-200 bg-white/95 backdrop-blur"><div className="mx-auto grid max-w-xl grid-cols-5">{tabs.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex min-h-16 flex-col items-center justify-center gap-1 text-xs ${isActive?'text-action':'text-zinc-500'}`}><Icon size={21}/><span>{label}</span></NavLink>)}</div></nav></div>}
